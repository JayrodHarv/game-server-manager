version: "3.9"

services:
  manager:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /srv/servers:/srv/servers
    restart: unless-stopped
