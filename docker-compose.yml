version: "3.9"

services:
  manager:
    build: .
    ports:
      - "80:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /srv/servers:/srv/servers
    restart: unless-stopped
